<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="TextAppearance">
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="TextAppearance.Title">
        <item name="android:textSize">15dp</item>
        <item name="android:textColor">@color/text_title</item>
        <item name="android:lineSpacingMultiplier">1.25</item>
        <item name="android:maxLines">3</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="TextAppearance.SubTitle">
        <item name="android:textSize">12dp</item>
        <item name="android:textColor">@color/text_subtitle</item>
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="TextAppearance.Description">
        <item name="android:textSize">12dp</item>
        <item name="android:textColor">@color/text_description</item>
        <item name="android:lineSpacingMultiplier">1.25</item>
        <item name="android:maxLines">2</item>
    </style>

    <style name="TextAppearance.Meta">
        <item name="android:textSize">11dp</item>
        <item name="android:textColor">@color/text_meta</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="TextAppearance.Provider">
        <item name="android:textSize">11dp</item>
        <item name="android:textColor">@color/text_meta</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="TextAppearance.Body">
        <item name="android:textSize">16dp</item>
        <item name="android:textColor">@color/text_body</item>
    </style>

    <style name="TextAppearance.ImageCaption">
        <item name="android:textSize">13dp</item>
        <item name="android:textColor">@color/red</item>
    </style>

    <style name="Widget.ProgressBar.Horizontal" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/progress_horizontal</item>
        <item name="android:indeterminateDrawable">@drawable/progress_horizontal</item>
        <item name="android:minHeight">@dimen/progress_bar_height</item>
        <item name="android:maxHeight">@dimen/progress_bar_height</item>
    </style>
    <style name="Widget.RecyclerView" parent="Widget.AppCompat.ListView">
        <item name="android:overScrollMode">never</item>
    </style>
</resources>
